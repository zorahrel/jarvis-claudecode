---
phase: 01-context-inspector
plan: 07
type: execute
wave: 3
depends_on: [01-06]
files_modified:
  - router/dashboard/src/hooks/useContextPolling.ts
  - router/dashboard/src/components/ContextTab.tsx
  - router/dashboard/src/pages/Context.tsx
  - router/dashboard/src/components/Sidebar.tsx
  - router/dashboard/src/App.tsx
autonomous: true
updated: 2026-05-01
requirements: [CTX-13]
must_haves:
  truths:
    - "Custom hook useContextPolling fetches /api/local-sessions + /api/sessions/cruft every 5s"
    - "Polling cleans up on unmount (no memory leak)"
    - "Polling pauses when tab is in background (document.hidden=true) to save resources"
    - "Polling errors are surfaced (last good data preserved, error displayed inline)"
    - "ContextTab consumes the hook instead of one-shot useEffect"
    - "New /context route wired in Sidebar + App router"
  artifacts:
    - path: "router/dashboard/src/hooks/useContextPolling.ts"
      provides: "useContextPolling(intervalMs) -> { data, cruft, loading, error, refresh }"
    - path: "router/dashboard/src/pages/Context.tsx"
      provides: "Page wrapper that renders <ContextTab />"
    - path: "router/dashboard/src/components/Sidebar.tsx (modified)"
      provides: "Adds Context entry in nav"
  key_links:
    - from: "ContextTab.tsx"
      to: "useContextPolling hook"
      via: "replaces inline useEffect with hook"
      pattern: "useContextPolling"
    - from: "App.tsx"
      to: "Context page route"
      via: "<Route path='context' /> entry"
      pattern: "Context"
---

<objective>
Wire polling (CTX-13: 5s auto-refresh, no memory leak after 10min) and route the Context tab as a first-class dashboard page (visible in Sidebar nav).

Three concerns:
1. **Custom hook `useContextPolling`** — encapsulates setInterval + cleanup + visibility-aware pause + error handling.
2. **ContextTab refactor** — replace inline `useEffect` one-shot fetch (from Plan 06) with the hook.
3. **Route wiring** — add a `/context` page entry to Sidebar + App router so the tab is reachable.

Output: 1 new hook + 1 new page + 3 file modifications. TS-clean. Manual smoke test post-build (in Plan 08).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-context-inspector/01-CONTEXT.md
@.planning/phases/01-context-inspector/01-06-SUMMARY.md

@/Users/zorahrel/.omnara/worktrees/jarvis/omnara/mural-polio/router/dashboard/src/hooks/usePolling.ts
@/Users/zorahrel/.omnara/worktrees/jarvis/omnara/mural-polio/router/dashboard/src/components/Sidebar.tsx
@/Users/zorahrel/.omnara/worktrees/jarvis/omnara/mural-polio/router/dashboard/src/App.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: useContextPolling hook + ContextTab refactor</name>
  <files>
    router/dashboard/src/hooks/useContextPolling.ts
    router/dashboard/src/components/ContextTab.tsx
  </files>
  <read_first>
    /Users/zorahrel/.omnara/worktrees/jarvis/omnara/mural-polio/router/dashboard/src/hooks/usePolling.ts (existing pattern in codebase to mirror)
    /Users/zorahrel/.omnara/worktrees/jarvis/omnara/mural-polio/router/dashboard/src/components/ContextTab.tsx (current Plan 06 implementation)
    /Users/zorahrel/.omnara/worktrees/jarvis/omnara/mural-polio/router/dashboard/src/api/client.ts (api.contextSessions, api.sessionsCruft)
    /Users/zorahrel/.claude/jarvis/.planning/phases/01-context-inspector/01-CONTEXT.md (Polling section)
    /Users/zorahrel/.claude/jarvis/.planning/phases/01-context-inspector/01-RESEARCH.md (Risk register: "Polling 5s causa memory leak — Test acceptance: heap snapshot dopo 10min")
  </read_first>
  <action>

**File 1 (NEW): `router/dashboard/src/hooks/useContextPolling.ts`**

```
import { useCallback, useEffect, useRef, useState } from "react";
import { api } from "../api/client";
import type { ContextSessionsResponse, CruftResponse } from "../api/client";

interface UseContextPollingResult {
  data: ContextSessionsResponse | null;
  cruft: CruftResponse | null;
  loading: boolean;
  error: string | null;
  refresh: () => Promise<void>;
  /** Wall-clock ms of last successful fetch. */
  lastFetchedAt: number | null;
}

/**
 * Polls /api/local-sessions + /api/sessions/cruft on the given interval.
 *
 * Memory-leak guards (CTX-13):
 *   - clearInterval on unmount
 *   - skip fetch when document.hidden (tab in background)
 *   - latestRequestId pattern: late responses from cancelled requests are discarded
 *   - last good data preserved when an individual fetch fails
 */
export function useContextPolling(intervalMs: number = 5000): UseContextPollingResult {
  const [data, setData] = useState<ContextSessionsResponse | null>(null);
  const [cruft, setCruft] = useState<CruftResponse | null>(null);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState<string | null>(null);
  const [lastFetchedAt, setLastFetchedAt] = useState<number | null>(null);

  const requestIdRef = useRef(0);
  const mountedRef = useRef(true);

  const refresh = useCallback(async () => {
    const myId = ++requestIdRef.current;
    try {
      const [sessionsResp, cruftResp] = await Promise.all([
        api.contextSessions(),
        api.sessionsCruft(),
      ]);
      // Discard late responses from cancelled or superseded requests
      if (!mountedRef.current || myId !== requestIdRef.current) return;
      setData(sessionsResp);
      setCruft(cruftResp);
      setError(null);
      setLastFetchedAt(Date.now());
    } catch (e) {
      if (!mountedRef.current || myId !== requestIdRef.current) return;
      setError(e instanceof Error ? e.message : "errore sconosciuto");
    } finally {
      if (mountedRef.current && myId === requestIdRef.current) setLoading(false);
    }
  }, []);

  useEffect(() => {
    mountedRef.current = true;
    refresh();
    const tick = () => {
      if (typeof document !== "undefined" && document.hidden) return;
      refresh();
    };
    const id = window.setInterval(tick, intervalMs);
    return () => {
      mountedRef.current = false;
      window.clearInterval(id);
    };
  }, [intervalMs, refresh]);

  return { data, cruft, loading, error, refresh, lastFetchedAt };
}
```

**File 2 (MODIFY): `router/dashboard/src/components/ContextTab.tsx`**

Replace the inline `useEffect` fetch with the new hook. Keep all the JSX exactly the same; only change:

1. Remove `useEffect` and `useState` from React imports entirely — all state management is now in the `useContextPolling` hook. The new ContextTab body uses zero `useState` calls (everything comes from the hook return value).
2. Remove the `fetchAll` function and the `useEffect`.
3. Add `import { useContextPolling } from "../hooks/useContextPolling";` at top.
4. Replace the body of the `ContextTab` function:

```
export default function ContextTab() {
  const { data, cruft, loading, error, refresh, lastFetchedAt } = useContextPolling(5000);

  if (loading && !data) {
    return <div style={{ padding: 24, color: "var(--text-3)" }}>Caricamento context inspector...</div>;
  }

  if (error && !data) {
    return (
      <div style={{ padding: 24, color: "var(--err, #ef4444)" }}>
        Errore: {error}
        <button onClick={refresh} style={{ marginLeft: 12 }}>Riprova</button>
      </div>
    );
  }

  if (!data) return null;

  return (
    <div style={{ padding: 16 }}>
      <div style={{ display: "flex", alignItems: "center", marginBottom: 12 }}>
        <h2 style={{ margin: 0, fontSize: 18, fontWeight: 600 }}>🧠 Context Inspector</h2>
        {error && (
          <span style={{ marginLeft: 12, fontSize: 11, color: "var(--err, #ef4444)" }}>
            ultimo fetch fallito: {error}
          </span>
        )}
        <span style={{ marginLeft: "auto", fontSize: 10, color: "var(--text-4)", marginRight: 8 }}>
          {lastFetchedAt ? `aggiornato ${Math.max(0, Math.floor((Date.now() - lastFetchedAt) / 1000))}s fa` : ""}
        </span>
        <button
          onClick={refresh}
          disabled={loading}
          style={{ display: "flex", alignItems: "center", gap: 4, padding: "4px 10px", background: "var(--bg-1)", border: "1px solid var(--border)", borderRadius: 6, cursor: "pointer" }}
        >
          <RefreshCw size={12} />
          <span style={{ fontSize: 11 }}>Aggiorna</span>
        </button>
      </div>

      <AggregateHeader aggregate={data.aggregate} disk={data.disk} />

      <div style={{ marginBottom: 16 }}>
        <div style={{ fontSize: 12, fontWeight: 600, color: "var(--text-2)", marginBottom: 8, textTransform: "uppercase", letterSpacing: 0.5 }}>
          Sessioni live
        </div>
        {data.sessions.length === 0 ? (
          <div style={{ color: "var(--text-3)", fontSize: 13, padding: 12 }}>Nessuna sessione attiva.</div>
        ) : (
          data.sessions.map((s) => <SessionRow key={s.pid} session={s} />)
        )}
      </div>

      <div style={{ marginBottom: 16 }}>
        <div style={{ fontSize: 12, fontWeight: 600, color: "var(--text-2)", marginBottom: 8, textTransform: "uppercase", letterSpacing: 0.5 }}>
          💡 Cruft / da pulire
        </div>
        <CruftPanel data={cruft} />
      </div>

      <RecentSessionsList sessions={data.recent} />
      <DiskHygieneFooter disk={data.disk} />
    </div>
  );
}
```

Keep all the existing imports for AggregateHeader/SessionRow/CruftPanel/RecentSessionsList/DiskHygieneFooter/RefreshCw. Remove now-unused imports of `useEffect`, `useState`, direct `api`, and the `ContextSessionsResponse`/`CruftResponse` types (these are owned by the hook now).

After changes, run `cd router/dashboard && npx tsc -b` and verify zero errors.
  </action>
  <verify>
    <automated>cd /Users/zorahrel/.omnara/worktrees/jarvis/omnara/mural-polio/router/dashboard && test -f src/hooks/useContextPolling.ts && grep -c "useContextPolling" src/hooks/useContextPolling.ts && grep -c "document.hidden" src/hooks/useContextPolling.ts && grep -c "clearInterval\|window.clearInterval" src/hooks/useContextPolling.ts && grep -c "requestIdRef" src/hooks/useContextPolling.ts && grep -c "useContextPolling" src/components/ContextTab.tsx && ! grep -q "useEffect" src/components/ContextTab.tsx && ! grep -q "useState" src/components/ContextTab.tsx && grep -c "lastFetchedAt" src/components/ContextTab.tsx && npx tsc -b 2>&1 | head -10</automated>
  </verify>
  <acceptance_criteria>
    - File `router/dashboard/src/hooks/useContextPolling.ts` exists
    - Hook contains literal `document.hidden` (visibility-aware pause)
    - Hook contains literal `clearInterval` or `window.clearInterval` (cleanup)
    - Hook contains literal `requestIdRef` (race condition guard)
    - Hook contains literal `mountedRef` (mount guard)
    - `router/dashboard/src/components/ContextTab.tsx` imports and calls `useContextPolling`
    - `ContextTab.tsx` does NOT contain any `useEffect` import (refactored away)
    - `ContextTab.tsx` does NOT contain any `useState` reference (all state is in the hook — MINOR 6 clarification)
    - `ContextTab.tsx` contains literal `lastFetchedAt` (shows freshness indicator in header)
    - `cd router/dashboard && npx tsc -b` reports zero errors
  </acceptance_criteria>
  <done>
Polling hook implemented with all 4 memory-leak guards (cleanup on unmount, visibility pause, request id race guard, mounted ref). ContextTab consumes the hook. CTX-13 satisfied at the code level — runtime test in Plan 08.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: New /context page route + Sidebar entry</name>
  <files>
    router/dashboard/src/pages/Context.tsx
    router/dashboard/src/components/Sidebar.tsx
    router/dashboard/src/App.tsx
  </files>
  <read_first>
    /Users/zorahrel/.omnara/worktrees/jarvis/omnara/mural-polio/router/dashboard/src/components/Sidebar.tsx (existing nav entries to mirror)
    /Users/zorahrel/.omnara/worktrees/jarvis/omnara/mural-polio/router/dashboard/src/App.tsx (existing routing pattern)
    /Users/zorahrel/.omnara/worktrees/jarvis/omnara/mural-polio/router/dashboard/src/pages/Sessions.tsx (one of the simpler page wrappers to mirror style)
    /Users/zorahrel/.omnara/worktrees/jarvis/omnara/mural-polio/router/dashboard/src/components/ContextTab.tsx
  </read_first>
  <action>

**File 1 (NEW): `router/dashboard/src/pages/Context.tsx`**

Minimal page wrapper. Mirrors the style of other pages in `src/pages/`.

```
import ContextTab from "../components/ContextTab";

export default function Context() {
  return <ContextTab />;
}
```

**File 2 (MODIFY): `router/dashboard/src/components/Sidebar.tsx`**

Inspect the existing structure (likely an array of nav entries with `{ id, label, icon, path }` or similar). Add a new entry for Context. The exact insertion strategy:

1. Open the file. Find the existing nav array/list. Identify the pattern.
2. Add a new entry with these properties (adapt to existing TypeScript shape):
   - `id: "context"` (or whatever id field the existing pattern uses)
   - `label: "Context"` (or the literal property name used)
   - `path: "/context"` or `"context"` depending on existing convention
   - `icon: <Brain size={14} />` from `lucide-react` (import `Brain` if not already)
3. Place it visually near the Sessions entry — same conceptual area (live state).
4. Keep all existing entries unchanged.

The exact TypeScript depends on the existing Sidebar shape — read the file first, follow its conventions exactly. Do not invent a new pattern.

**File 3 (MODIFY): `router/dashboard/src/App.tsx`**

Add the route to the Context page. Inspect the existing routing pattern (likely a switch on a `currentPage` state, or `<Route />` JSX). Follow existing conventions exactly.

If the file uses a `Routes`/`Route` JSX pattern (react-router style):
```
<Route path="/context" element={<Context />} />
```

If it uses a state-machine switch (current page string), add a case for `"context"` returning `<Context />` and ensure the navigation logic (clicking the Sidebar entry sets `currentPage = "context"`).

Either way: the Sidebar Context entry must navigate to a state where `<Context />` renders. Test this by running `npm run build` after changes — TS errors will catch most mistakes.

After all three file changes, run `cd router/dashboard && npx tsc -b` and verify zero errors.

**Important:** do NOT remove or modify the existing Sessions tab — it continues to render `LocalSessionsSection` for process-level operations.
  </action>
  <verify>
    <automated>cd /Users/zorahrel/.omnara/worktrees/jarvis/omnara/mural-polio/router/dashboard && test -f src/pages/Context.tsx && grep -c "ContextTab" src/pages/Context.tsx && grep -c -i "context" src/components/Sidebar.tsx && grep -c -i "Context" src/App.tsx && npx tsc -b 2>&1 | head -10</automated>
  </verify>
  <acceptance_criteria>
    - File `router/dashboard/src/pages/Context.tsx` exists
    - File contains literal `import ContextTab` and `export default function Context`
    - `router/dashboard/src/components/Sidebar.tsx` contains the literal string `Context` (the new nav entry — case-sensitive, capitalized as a label)
    - `router/dashboard/src/App.tsx` contains the literal string `Context` (the new route reference)
    - `cd router/dashboard && npx tsc -b` reports zero errors
    - The existing Sessions tab is still wired (no regression — search for `Sessions` in Sidebar.tsx to confirm presence)
  </acceptance_criteria>
  <done>
Context tab is reachable from the dashboard nav. Plan 08 builds the dashboard and restarts the router so the new tab is visible at `localhost:3340`.
  </done>
</task>

</tasks>

<verification>
- All 5 file changes present (1 new hook + 1 new page + 3 modifications)
- `cd router/dashboard && npx tsc -b` reports zero errors
- ContextTab uses `useContextPolling` hook (no inline `useEffect`)
- Sidebar has Context entry; App routes `/context` to the page
- Existing Sessions tab still works (no regression)
</verification>

<success_criteria>
- 5s polling with all memory-leak guards (CTX-13 implemented)
- Context tab reachable from dashboard nav
- Visibility-aware pause prevents unnecessary fetches when tab in background
- Last good data preserved when individual fetches fail (graceful degradation)
- TypeScript clean
</success_criteria>

<output>
After completion, create `.planning/phases/01-context-inspector/01-07-SUMMARY.md` documenting:
- Hook API: `useContextPolling(intervalMs) -> { data, cruft, loading, error, refresh, lastFetchedAt }`
- Memory-leak guards used (cleanup, visibility, race-id, mount-ref)
- Sidebar/App.tsx integration pattern (note exact pattern used so it can be replicated for future tabs)
- Hand-off to Plan 08: how to verify polling does not leak (Chrome DevTools heap snapshot procedure)
</output>
