---
phase: 01-context-inspector
plan: 08
type: execute
wave: 4
depends_on: [01-05, 01-07]
files_modified: []
autonomous: false
updated: 2026-05-01
requirements: [CTX-01, CTX-02, CTX-03, CTX-04, CTX-05, CTX-06, CTX-07, CTX-08, CTX-09, CTX-10, CTX-11, CTX-12, CTX-13, CTX-14, CTX-15]
must_haves:
  truths:
    - "All Wave 1 unit tests pass via `bash router/scripts/run-context-tests.sh`"
    - "TypeScript clean across both router/ and router/dashboard/"
    - "Dashboard built successfully via `npm run build` in router/dashboard/"
    - "Router restarted via launchctl after warning user about active queries"
    - "All 3 endpoints respond with the documented JSON shapes (curl smoke tests)"
    - "Dashboard tab Context renders with live data, threshold colors, breakdown drill-down"
    - "10-minute polling smoke test shows no memory leak (heap snapshot delta < 5 MB)"
  artifacts: []
  key_links: []
---

<objective>
Final verification + ship. Run all automated tests, ensure TS clean, build the dashboard, restart the router (with the active-query warning per user memory `feedback_router_restart_inflight`), then run a manual visual + functional verification checklist mapping each CTX-XX requirement to a verifiable observation.

This plan is NOT autonomous — it includes a checkpoint:human-verify task because UI behavior + memory-leak verification require visual inspection by the user.

Output: A deployed working Context tab + a SUMMARY.md mapping every CTX-XX to a green check.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-context-inspector/01-CONTEXT.md
@.planning/phases/01-context-inspector/01-RESEARCH.md
@.planning/phases/01-context-inspector/01-VALIDATION.md
@.planning/phases/01-context-inspector/01-05-SUMMARY.md
@.planning/phases/01-context-inspector/01-07-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Final automated test sweep + TS check + dashboard build</name>
  <files></files>
  <read_first>
    /Users/zorahrel/.claude/jarvis/.planning/phases/01-context-inspector/01-VALIDATION.md (sampling rate + commands)
    /Users/zorahrel/.omnara/worktrees/jarvis/omnara/mural-polio/CLAUDE.md (project rule: dashboard build before restart)
  </read_first>
  <action>

Run the following commands in sequence. ALL must exit 0 before proceeding.

**Step 1 — Backend unit + integration tests:**

```bash
cd /Users/zorahrel/.omnara/worktrees/jarvis/omnara/mural-polio/router
bash scripts/run-context-tests.sh
```

Capture the tap output. Expected: at least 30 passing tests (5+ tokenSource + 7+ cost + 5+ claudeMdChain + 6+ breakdown + 6+ jsonlParser + 6+ cruft + 6+ diskHygiene + 4+ api integration). Zero failures.

If any test fails, STOP and report which test + the failure message — do NOT proceed to build/restart.

**Step 2 — Backend TypeScript check:**

```bash
cd /Users/zorahrel/.omnara/worktrees/jarvis/omnara/mural-polio/router
npx tsc --noEmit
```

Expected: zero errors. Capture stderr.

**Step 3 — Dashboard TypeScript check:**

```bash
cd /Users/zorahrel/.omnara/worktrees/jarvis/omnara/mural-polio/router/dashboard
npx tsc -b
```

Expected: zero errors.

**Step 4 — Dashboard build:**

```bash
cd /Users/zorahrel/.omnara/worktrees/jarvis/omnara/mural-polio/router/dashboard
npm run build
```

Expected: vite build success message; `router/dashboard/dist/` exists with `index.html` and `assets/`.

If build fails, STOP and report the error.

After all 4 steps succeed, report a clear summary:
- Tests: N passing, 0 failures
- Backend TS: clean
- Dashboard TS: clean
- Dashboard build: dist/ ready
  </action>
  <verify>
    <automated>cd /Users/zorahrel/.omnara/worktrees/jarvis/omnara/mural-polio/router && bash scripts/run-context-tests.sh 2>&1 | tail -3 && cd /Users/zorahrel/.omnara/worktrees/jarvis/omnara/mural-polio/router && npx tsc --noEmit 2>&1 | head -3 && cd /Users/zorahrel/.omnara/worktrees/jarvis/omnara/mural-polio/router/dashboard && npx tsc -b 2>&1 | head -3 && test -f /Users/zorahrel/.omnara/worktrees/jarvis/omnara/mural-polio/router/dashboard/dist/index.html && echo "BUILD OK"</automated>
  </verify>
  <acceptance_criteria>
    - `bash router/scripts/run-context-tests.sh` exits 0 with tap summary including `# pass` count >= 30 and `# fail 0`
    - `npx tsc --noEmit` from `router/` produces no error output
    - `npx tsc -b` from `router/dashboard/` produces no error output
    - `npm run build` from `router/dashboard/` completes successfully
    - File `router/dashboard/dist/index.html` exists after build
    - Verify command output ends with `BUILD OK`
  </acceptance_criteria>
  <done>
All automated checks green; dashboard built. Ready for restart.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Restart router (with active-query warning) + smoke tests</name>
  <files></files>
  <read_first>
    /Users/zorahrel/.omnara/worktrees/jarvis/omnara/mural-polio/CLAUDE.md (restart command)
    /Users/zorahrel/.claude/projects/-Users-zorahrel--claude-jarvis/memory/MEMORY.md (look for feedback_router_restart_inflight — warn user about active queries first)
  </read_first>
  <what-built>
A new dashboard tab "Context" with:
- Aggregate header: N sessioni, total token, costo medio per turno
- Per-session list with progress bar colored 50/75/90 thresholds + cost + compaction count
- Click-to-expand breakdown showing 8 categories (system_preset, builtin_tools, mcp_servers, skills_index, claudemd_chain, subagents, hooks_memory, history)
- Cruft panel with per-agent unused MCP/skill findings + suggestions
- Recent sessions list (last 10 closed)
- Disk hygiene footer (MB, JSONL count, files >30g)
- 5s auto-refresh with memory-leak guards

Backend changes:
- New `/api/sessions/:id/breakdown` endpoint
- New `/api/sessions/cruft` endpoint
- Extended `/api/local-sessions` with token + cost + compaction + sessionKey fields (legacy shape via `?legacy=1`)
- claude.ts SDK consumer loop captures `task_progress.usage.total_tokens` for live signal
  </what-built>
  <how-to-verify>

**Pre-restart safety check (per user memory `feedback_router_restart_inflight`):**

1. Check active queries before kickstart — the kickstart will kill in-flight responses. Run:
   ```bash
   curl -s http://127.0.0.1:3340/api/local-sessions?legacy=1 | jq '[.[] | select(.status == "working" or .status == "waiting")] | length'
   ```
   If the count > 0, ASK the user:
   > "Ci sono N query attive ora (notch/TG/WA). Il restart le killa. Procedo o aspetto?"
   Wait for explicit "procedi" before continuing.

2. **Restart the router:**
   ```bash
   launchctl kickstart -k gui/$(id -u)/com.jarvis.router
   ```

3. **Wait for boot + verify:**
   ```bash
   sleep 3
   curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:3340/api/local-sessions
   ```
   Expected: HTTP 200.

**Smoke tests (run each, attach output to verification):**

4. Aggregate endpoint shape:
   ```bash
   curl -s http://127.0.0.1:3340/api/local-sessions | jq '{aggregate, disk: .disk, recent_count: (.recent | length), session_count: (.sessions | length)}'
   ```
   Expected JSON keys: `aggregate.totalSessions`, `aggregate.totalLiveTokens`, `aggregate.avgCostPerTurnUsd`, `disk.totalMb`, `disk.totalJsonl`, `disk.filesOlderThan30d`, `recent_count`, `session_count`.

5. Cruft endpoint shape:
   ```bash
   curl -s http://127.0.0.1:3340/api/sessions/cruft | jq '.agents | length'
   ```
   Expected: a number (>= 0).

6. Breakdown endpoint shape — pick a real sessionId from the local-sessions response, then:
   ```bash
   SID=$(curl -s http://127.0.0.1:3340/api/local-sessions | jq -r '.sessions[0].sessionId // empty')
   if [ -n "$SID" ]; then curl -s "http://127.0.0.1:3340/api/sessions/$SID/breakdown" | jq '{categories_count: (.categories | length), totalEstimated, liveTotal, agent}'; fi
   ```
   Expected: `categories_count: 8`, plus all 4 numeric/string fields.

7. Legacy shape compatibility:
   ```bash
   curl -s "http://127.0.0.1:3340/api/local-sessions?legacy=1" | jq 'type'
   ```
   Expected: `"array"`.

**Visual UI verification (open in browser):**

8. Open http://127.0.0.1:3340 in your browser.
9. Click "Context" in the Sidebar. Tab loads.
10. Verify:
    - [ ] Aggregate header shows N sessioni + total token + costo medio (italian labels)
    - [ ] At least 1 session row visible with colored progress bar
    - [ ] Threshold colors verified at the boundaries (BLOCKER 2 fix — was green, now yellow at 50-74%):
          - blue (#3b82f6) for sessions < 50% full
          - yellow (#eab308) for sessions 50-74% (NOT green — green→yellow transition per CONTEXT.md)
          - orange (#f97316) for sessions 75-89%
          - red (#ef4444) for sessions ≥90%
          (If no live session naturally falls in 50-74%, eyeball at least the safe blue + the yellow tier — refresh until visible, or check `colorForThreshold(0.6)` returns `#eab308` in DevTools console)
    - [ ] Click on a session row → drill-down expands → 8-category stacked bar visible with italian category labels (System preset, Tool integrati, MCP servers, Skills index, CLAUDE.md chain, Subagents, Hooks/Memory, Conversation history)
    - [ ] For jarvis (if live): MCP category shows ~10-15k tokens with 13 server names listed below
    - [ ] CLAUDE.md drill-down shows file paths with token counts
    - [ ] Cruft panel shows per-agent findings with italian labels (MCP non chiamati, Skill mai invocate, Suggerimenti)
    - [ ] Cruft panel: "Skill mai invocate" section is EXPECTED to be empty in v1 (CTX-09 caveat — skills marketplace index reader is deferred to v2 per Plan 04 must_haves). Production skill cruft will fire as soon as Plan 05 enriches `skillsLoaded` (currently `[]` in the API handler).
    - [ ] Recent sessions list shows entries with `g fa` (giorni fa) time format
    - [ ] Disk footer shows MB, JSONL count, filesOlderThan30d count
    - [ ] Header shows "aggiornato Ns fa" indicator updating live (every 5s)
    - [ ] Sidecar mechanism (MAJOR 5 fix) is working — at least one router-spawned session in the API response has a non-null `sessionKey` field. Manual verify command:
          ```bash
          curl -s http://127.0.0.1:3340/api/local-sessions | jq '.sessions[] | select(.isRouterSpawned == true) | {pid, sessionKey, agent, lastTurnCostUsd}'
          ```
          Expected: at least one entry with `sessionKey: "telegram:...:..."` (or similar) AND a non-zero `lastTurnCostUsd` (BLOCKER 1 fix — proves the `result` event tap populated lastTurnUsage so cost calc worked).
    - [ ] Confirm sidecar files exist on disk for active router sessions:
          ```bash
          ls -la ~/.claude/jarvis/active-sessions/
          ```
          Expected: one `<pid>.json` file per active router-spawned Claude SDK process. Each file's content should be valid JSON containing `sessionKey`, `agent`, `workspace`, `model`.

**Memory-leak verification (CTX-13):**

11. Open Chrome DevTools → Memory tab → "Take heap snapshot". Note the size.
12. Leave the Context tab open for 10 minutes. Periodically click around (expand sessions, refresh).
13. Take another heap snapshot. Verify retained delta < 5 MB.
14. While the dashboard is open, switch to another browser tab for 1 minute, then come back. Verify no fetches happened during background time (check Network panel for the gap).

**Resume signal:** Type one of:
- `approved` — all checks pass, mark phase complete
- `failed: <description>` — describe what's broken; orchestrator will route to gap closure plans
  </how-to-verify>
  <resume-signal>Type "approved" if all verifications pass. Else "failed: <list of failed items>"</resume-signal>
  <acceptance_criteria>
    - User confirmed by typing `approved`
    - All 7 smoke tests (curl) returned the expected shapes
    - All visual UI checks passed (including BLOCKER 2 yellow threshold + MAJOR 5 sidecar `sessionKey` + BLOCKER 1 non-zero `lastTurnCostUsd` for router-spawned sessions)
    - Skill cruft section is empty as expected (CTX-09 v1 caveat documented)
    - Memory leak test (10min): retained heap delta < 5 MB
    - Visibility-aware pause confirmed (no fetches when tab in background)
  </acceptance_criteria>
  <done>
Phase 1 — Context Inspector — verified end-to-end. Ready for SUMMARY commit.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Write phase SUMMARY and update ROADMAP/STATE</name>
  <files>
    .planning/phases/01-context-inspector/01-08-SUMMARY.md
    .planning/STATE.md
    .planning/ROADMAP.md
  </files>
  <read_first>
    /Users/zorahrel/.claude/jarvis/.planning/STATE.md
    /Users/zorahrel/.claude/jarvis/.planning/ROADMAP.md
    /Users/zorahrel/.claude/jarvis/.planning/REQUIREMENTS.md
  </read_first>
  <action>

**File 1 (NEW): `.planning/phases/01-context-inspector/01-08-SUMMARY.md`**

Produce a phase-completion summary with the following sections:

1. **Header** — phase id, completion date, total plans (8), total Wave count (4)
2. **Requirements coverage table** — one row per CTX-01..CTX-15 with: requirement description, "Implemented in Plan(s) NN", "Verified by" (test command or visual check)
3. **Files changed** — list all files created/modified with one-line description
4. **Test stats** — total passing tests, total runtime, paths to spec files
5. **Manual verification log** — checklist from Task 2 with checked boxes
6. **Known caveats / followups for Phase 2** — items deferred (M4 push alerts, M5 tray app, M6 disk hygiene wizard, M7 config cleanup)
7. **Memory leak baseline** — heap snapshot delta measured during 10min test
8. **Next steps** — pointer to "evidence-based config cleanup" — what knob to turn first based on what the dashboard now shows

**File 2 (MODIFY): `.planning/STATE.md`**

Update sections:
- `Current Position`:
  - Phase: change from "1 of 1" to "1 complete"
  - Plan: "8 of 8"
  - Status: "Complete"
  - Last activity: today's date with note "Phase 1 ContextInspector shipped"
- `Progress`: change `[░░░░░░░░░░] 0%` to `[██████████] 100%`
- `Decisions`: append a new dated entry referencing the phase outcome
- `Pending Todos`: keep the existing CLAUDE.md drift fix item (still applicable)
- `Session Continuity`: update `Stopped at` and `Resume file` to reflect completion

**File 3 (MODIFY): `.planning/ROADMAP.md`**

- Change `- [ ] Phase 1: Context Inspector` to `- [x] Phase 1: Context Inspector`
- Update Progress table: `Plans Complete: 8/8`, `Status: Complete`, `Completed: <today>`
- The Plans list under Phase 1 — replace the `01-01: TBD` placeholder with the 8 actual plan titles
  </action>
  <verify>
    <automated>test -f /Users/zorahrel/.claude/jarvis/.planning/phases/01-context-inspector/01-08-SUMMARY.md && grep -c "CTX-01" /Users/zorahrel/.claude/jarvis/.planning/phases/01-context-inspector/01-08-SUMMARY.md && grep -c "CTX-15" /Users/zorahrel/.claude/jarvis/.planning/phases/01-context-inspector/01-08-SUMMARY.md && grep -c "Phase 1.*Complete\|██████████" /Users/zorahrel/.claude/jarvis/.planning/STATE.md && grep -c "x\] \*\*Phase 1" /Users/zorahrel/.claude/jarvis/.planning/ROADMAP.md && echo "DOCS OK"</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/01-context-inspector/01-08-SUMMARY.md` exists
    - SUMMARY contains all 15 CTX-XX requirement IDs
    - SUMMARY contains a "Files changed" section listing at least 25 files (Wave 0 fixtures + Wave 1 source + Wave 2 modifications + Wave 3 components)
    - SUMMARY contains a "Memory leak baseline" section with the measured delta
    - `.planning/STATE.md` contains the literal `██████████` progress bar (or `100%`) and "Phase 1 ContextInspector shipped" note
    - `.planning/ROADMAP.md` contains `[x] **Phase 1: Context Inspector**` (Phase 1 marked complete)
    - Verify command output ends with `DOCS OK`
  </acceptance_criteria>
  <done>
Phase 1 documentation complete. STATE/ROADMAP/SUMMARY all reflect ship status. Ready for git commit.
  </done>
</task>

</tasks>

<verification>
- All automated tests pass (Task 1)
- Router restarted; all 3 endpoints respond correctly (Task 2 smoke tests)
- All 14 manual UI/memory checks pass (Task 2 checklist)
- Documentation updated (Task 3)
</verification>

<success_criteria>
- All 15 requirements (CTX-01..CTX-15) have evidence in SUMMARY mapping them to test/visual verification
- User has typed `approved` after verifying the dashboard
- Phase 1 marked complete in ROADMAP + STATE
- Memory leak test passed (delta < 5 MB after 10 min)
- All TypeScript clean across both router and dashboard
- Italian labels confirmed in UI per user preference
</success_criteria>

<output>
The 01-08-SUMMARY.md created in Task 3 IS the phase summary. After completion, the orchestrator will commit all changes including planning docs.

Suggested commit message (italiano per user preference, no Co-Authored-By Claude per user feedback):

```
feat(dashboard): aggiunge tab Context Inspector con breakdown e cruft detection

- Nuovo tab Context: lista live sessioni con bar % e threshold colore 50/75/90
- Drill-down 8 categorie (system, tools, MCP, skills, CLAUDE.md, subagents, hooks, history)
- Cruft detection: MCP non chiamati + skill non invocate + suggerimenti config
- Endpoint nuovi: /api/sessions/:id/breakdown, /api/sessions/cruft
- /api/local-sessions esteso con liveTokens + lastTurnCost + compactionCount (legacy via ?legacy=1)
- Tap SDK task_progress.usage.total_tokens per token live router-spawned
- JSONL tail-read per token live bare CLI
- Polling 5s con memory-leak guards (cleanup, visibility-pause, race-id)

Closes Phase 1 Context Inspector (CTX-01..CTX-15).
```
</output>
