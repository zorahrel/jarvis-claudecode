/**
 * Vitest setup — runs once before each test file.
 *
 * Enables `@testing-library/jest-dom` matchers (toBeInTheDocument,
 * toHaveTextContent, etc.) on the global `expect`.
 */
import '@testing-library/jest-dom/vitest'
