{
  "name": "mcp-distilled",
  "version": "1.0.0",
  "description": "MCP server exposing distilled workflows (auto-learned skills) as tools. Each live distillate under ~/.claude/jarvis/distilled/<name>/ becomes one tool.",
  "type": "module",
  "main": "index.mjs",
  "scripts": {
    "start": "node index.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0"
  }
}
